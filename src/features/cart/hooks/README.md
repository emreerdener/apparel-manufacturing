# Cart Hooks

_This directory is currently empty._

Future hooks will include:

- `useCart`: Main hook to access cart context (items, itemCount, total).
- `useCartActions`: Helper functions to add/remove/update items.
