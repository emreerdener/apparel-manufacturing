# Cart Feature

This directory encapsulates all logic, state, and UI related to the shopping cart functionality.

## Structure

- `components/`: React components for displaying cart items, totals, and the cart drawer/page.
- `hooks/`: Custom hooks for managing cart state (add, remove, update quantities).
- `types/`: TypeScript definitions for cart items and shared interfaces.
