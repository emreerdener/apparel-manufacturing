# Layout Feature

This directory manages the global application layout, including the shell, navigation, header, and footer.

## Structure

- `components/`: Shared structural components like `Header`, `Footer`, and `MainAppShell`.
