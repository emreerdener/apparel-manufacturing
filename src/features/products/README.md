# Products Feature

This directory is the core of the e-commerce functionality, handling product browsing, searching, and details.

## Structure

- `components/`: UI components for product lists, cards, and sidebars.
- `services/`: API clients (SageClient) for fetching product data.
- `types/`: Shared TypeScript interfaces for product data structures.
